## 1. Distribution Contract

- [x] 1.1 Replace bare package-index installation examples with GitHub direct references for the base library and `cli` extra, and retain repository metadata identifying the GitHub source.
- [x] 1.2 Add executable behavior coverage proving supported installation guidance is GitHub-only while required Python build metadata remains present.

## 2. Verification

- [x] 2.1 Verify the CLI extra through a direct repository requirement and run the complete Behave and pytest suites.
- [x] 2.2 Validate the completed OpenSpec change strictly before canonical synchronization.
